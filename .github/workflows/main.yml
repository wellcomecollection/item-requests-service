name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Request API Tests
      run: make requests_api-test
    - name: Items API Tests
      run: make items_api-test
